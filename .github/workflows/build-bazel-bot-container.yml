name: Build Bazel Bot Container

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/build-bazel-bot-container.yml
      - 'google-bazel-bot/docker/**'
      - 'google-bazel-bot/bazel-bot/**'
  pull_request:
    paths:
      - .github/workflows/build-bazel-bot-container.yml
      - 'google-bazel-bot/docker/**'
      - 'google-bazel-bot/bazel-bot/**'

jobs:
  build-container:
    if: github.repository_owner == 'llvm'
    runs-on: ubuntu-24.04
    strategy:
      matrix:
       container-name:
         - 'bazel-buildkite'
         - 'bazel-fixer-bot'
    steps:
      - name: Checkout LLVM Zorg
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          sparse-checkout: google-bazel-bot/
      - name: Build Container
        uses: llvm/llvm-project/.github/actions/build-container@12e957fd7feb388b266fd71ab7960450df6884c1
        with:
          container-name: ${{ matrix.container-name }}
          context: google-bazel-bot/
          target: ${{ matrix.container-name }}
          dockerfile: google-bazel-bot/docker/Dockerfile

  push-container:
    if: github.event_name == 'push'
    needs:
      - build-container
    permissions:
      packages: write
    runs-on: ubuntu-24.04
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: llvm/llvm-project/.github/actions/push-container@12e957fd7feb388b266fd71ab7960450df6884c1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
