apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: advisor-db-pvc
  namespace: premerge-advisor
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: standard-rwo
